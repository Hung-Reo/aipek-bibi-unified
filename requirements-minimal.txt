# ===================================================================
# MINIMAL REQUIREMENTS - WORKING VERSION FOR SECURITY FIXES
# ===================================================================
# Last tested: 2025-11-05
# Platform: macOS (Python 3.11)
# Status: ✅ VERIFIED WORKING
#
# This is the MINIMAL set of dependencies needed to run the server
# with all 4 CRITICAL security fixes. Tested and verified working.
# ===================================================================

# Core Web Framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# Security Features (NEW)
slowapi>=0.1.9              # ✅ Rate limiting

# AI Services
openai>=1.74.0
pinecone-client>=3.0.0      # Note: Use 'pinecone[grpc]' on newer systems

# LangChain Stack (COMPLETE - required for retriever.py)
langchain>=0.1.0
langchain-community>=0.0.30  # ✅ Required for embeddings
langchain-core>=0.1.52       # ✅ Required for langchain
langchain-openai>=0.0.8
langchain-pinecone>=0.1.2
langchain-text-splitters     # ✅ Required for RecursiveCharacterTextSplitter

# Utilities
python-dotenv>=1.0.0
jinja2>=3.1.0
aiofiles>=23.0.0
aiohttp>=3.8.0

# ===================================================================
# INSTALLATION INSTRUCTIONS
# ===================================================================
#
# Option 1: Install minimal (fastest)
# ------------------------------------
# pip install -r requirements-minimal.txt
#
# Option 2: Install specific versions (most stable)
# -------------------------------------------------
# deactivate && rm -rf .venv
# python3.11 -m venv .venv
# source .venv/bin/activate
# pip install --upgrade pip
# pip install fastapi uvicorn slowapi openai pinecone-client \
#   langchain langchain-community langchain-core langchain-openai \
#   langchain-pinecone langchain-text-splitters \
#   pydantic python-dotenv jinja2 aiofiles aiohttp
#
# Option 3: If pinecone-client fails (newer Macs)
# -----------------------------------------------
# Replace pinecone-client with:
# pip install "pinecone[grpc]>=7.0.0"
# And update langchain versions:
# pip install langchain==0.3.0 langchain-pinecone==0.2.0
#
# ===================================================================
# TROUBLESHOOTING
# ===================================================================
#
# Issue: "No module named 'slowapi'"
# → pip install slowapi
#
# Issue: "No module named 'langchain_community'"
# → pip install langchain-community
#
# Issue: "No module named 'langchain.text_splitter'"
# → pip install langchain-text-splitters
# → Edit app/core/retriever.py:
#   from langchain_text_splitters import RecursiveCharacterTextSplitter
#
# Issue: "pinecone-client conflict"
# → pip uninstall pinecone-client
# → pip install "pinecone[grpc]"
#
# ===================================================================

# OPTIONAL: Full features (if needed)
# ===================================================================
# Uncomment below if you need document processing, admin UI, etc.
# -------------------------------------------------------------------
# streamlit>=1.44.0
# pymupdf>=1.22.5
# pypdf>=3.15.1
# pdfplumber>=0.10.2
# numpy>=1.24.0
# pandas>=2.0.0
# scikit-learn>=1.6.0
# matplotlib>=3.0.0
# sentence-transformers>=2.2.2
# ===================================================================
